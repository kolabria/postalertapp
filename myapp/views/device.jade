extends layout

block content

  h3 Devices
  p Currently registered with your account
  p ID: #{user.deviceID}
  br
  br
  p Enter a device ID to register a device with your account
  form(role='form', action="/device",method="post", style='max-width: 300px;')
    .form-group
        input.form-control(type='text', name="deviceid", placeholder="Enter a valid device ID")
    button.btn.btn-default(type='submit') Submit
  